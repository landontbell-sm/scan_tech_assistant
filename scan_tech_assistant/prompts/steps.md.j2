{% if show_target_legend %}
**Before running:** fill in `<TARGET>` with the address this finding gave you — paste it in as one piece, don't split it up. For most findings that's the exact flagged request straight from the finding's own output (Nessus often reports the specific vulnerable parameter and request itself); otherwise it's the host[:port]/path the finding points at.

{% endif %}
{% for step in steps %}
### Step {{ step.order }}: {{ step.title }}
{{ step.explanation }}
{% if step.command %}
*({{ "plugin's own payload" if step.payload_origin == "plugin" else "model-designed payload" }})*
```bash
{{ step.command }}
```
{% endif %}
{% for outcome in step.outcomes %}
- **If:** {{ outcome.observation }}
  **Means:** {{ outcome.meaning }}
  **Next:** {{ outcome.next_action }}
{% endfor %}

{% endfor %}
{% if note %}
**Note:** {{ note }}

{% endif %}
---
*Research aid only — not an assessment record. Sensitive command output (e.g. dumped file contents) should not be pasted into tickets.*
